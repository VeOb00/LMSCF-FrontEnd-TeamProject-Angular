import { PortfolioFiltersPipe } from './portfolio-filters.pipe';

describe('PortfolioFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PortfolioFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
